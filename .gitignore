node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vite
*.log
*.tsbuildinfo

# Saved logged-in browser sessions (storageState) for channel gathering
.rushhour/
.vercel
.claude/worktrees/
# Page snapshots the Playwright MCP browser writes while verifying a change by hand. Debris from
# testing, not source, and a `git add -A` during a verification run swept eleven of them into a PR.
.playwright-mcp/
